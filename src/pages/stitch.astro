---
import Layout from "../layouts/Layout.astro";
import App from "$lib/tools/stitch/App.svelte";
---

<Layout
  title="Stitch — close seams in a tiled polygon layer in your browser"
  description="Browser-only tool that closes seams between independently-computed tiles in a polygon layer with a single coverage-clean pass."
>
  <App client:load />
</Layout>
