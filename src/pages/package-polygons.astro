---
import Layout from "../layouts/Layout.astro";
import App from "$lib/tools/package-polygons/App.svelte";
---

<Layout
  title="Package Polygons: dissolve a boundary layer up every admin level in your browser"
  description="Browser-only tool that dissolves a polygon layer up to every coarser admin level it contains, one output file per level."
>
  <App client:load />
</Layout>
